The purpose of this project was to use Javascrip to create populate an HTML table with data.
Additionally, Javascript was used to create a date search filter. 

To populate teh HTML table, a function called "populateTable" was created. 
This function takes data in the form of an array of ojbects. It loops through
the array, appending a new row for each objecy. Then, for each value in the object, 
data is added to the new row. 

The search filter function is called upon when the "filter table" button on the HTML page
is clicked. Once that event occurs, the value of the input element is stored as a variable.
That variable gets used for 


The data used was a collection of UFO sightings. 

